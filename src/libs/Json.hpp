class json{ 
      public:
            void Parse(FILE sourse_file);
      private:
            std::vector<int> key;
            std::vector<std::string> value;

};
void json::Parse(FILE sourse_file){
      
}
